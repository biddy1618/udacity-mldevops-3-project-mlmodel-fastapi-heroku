import setuptools

setuptools.setup(
    name="local_project",
    version="0.0.0",
    description="Pipeline for building .",
    author="Dauren Baitursyn",
    packages=setuptools.find_packages()
)
